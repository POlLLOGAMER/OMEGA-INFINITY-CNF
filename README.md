# OMEGA-INFINITY-CNF

First of all, in Google Colab, run the following command:

!python3 parse_solution.py sha3_var.map solution.txt

Download `sha3_var.map` from this repository, and obtain `solution.txt` from the SAT Competition benchmark after the instance has been successfully solved.

Run the command, and that's it.
